- hosts: all
  vars:
    brew_bin_dir: /home/linuxbrew/.linuxbrew/bin
    brew: '{{brew_bin_dir}}/brew'

  roles:
    # Package managers
    - apt
    - homebrew
    - asdf

    # Core utils
    - bash
    - starship
    - kitty

    # Development
    - neovim
    - tmux

    # Other
    - chrome
